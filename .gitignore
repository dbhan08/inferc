# Private — never push
RESUME.md
WALKTHROUGH.md
WALKTHROUGH.pdf

# Paper drafts and figures — local-only until submission decision
docs/PAPER_DRAFT.md
docs/PAPER_*.md
docs/PAPER_*.pdf
docs/PAPER2*.md
docs/PAPER2*.pdf
docs/fig_*.png
docs/fig_*.pdf
docs/build_paper.sh
docs/build_paper2*.sh
docs/arxiv_submission/
docs/arxiv_submission.tar.gz

# Local agent config / skills
.claude/

# Build artifacts
build/
*.o
*.a
*.dylib

# Python venv + caches (Poetry uses ~/Library/Caches/pypoetry by default — these cover in-project mode)
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Local secrets / env
.env
.env.local

# Models, binary inputs, and golden artifacts — all regenerable via scripts/
# (keeps the 268MB DistilBERT ONNX out of git)
models/

# Editor / OS noise
.DS_Store
.vscode/
.idea/
*.swp
.cache/
Testing/

# Profiling output
*.json.gz
profile_*.json
# Local bench artifacts (regenerable via `inferc bench`)
bench_out/

# Tahoe 26 re-measurement campaign (local-only)
docs/results_tahoe26/
